.comment
  xif('$data.avatar')
    avatar-img(attrs='$data.avatar')
  .content
    a.author Matt
    xif('$data.meta')
      .metadata
        span(class='$data.meta.ui') $data.meta.label
    .text
      | $data.text
    xif('$data.actions')
      .actions
        for(each='action in data.actions')
          a(class='$action.ui') $action.label
